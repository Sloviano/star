package com.starlink.scanner.ui.theme

import androidx.compose.ui.graphics.Color

// Brand / accent — strong blue (spec design token).
val StarlinkBlue = Color(0xFF1565C0)
val StarlinkBlueDark = Color(0xFF5E92F3)

// Semantic status colors (upload state + dish connection).
val StatusGreen = Color(0xFF2E7D32) // success / SENT / connected
val StatusAmber = Color(0xFFF9A825) // pending / searching
val StatusRed = Color(0xFFC62828)   // error / FAILED

// Neutral surfaces tuned for high outdoor contrast.
val SurfaceLight = Color(0xFFFDFDFD)
val SurfaceDark = Color(0xFF121212)
